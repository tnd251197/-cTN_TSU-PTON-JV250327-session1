// cho người dùng nhập câu trả lời.
let answer = prompt("What is the “official” name of JavaScript?");
// đặt điều kiện và hiển thị ra màn hình.
    if(answer === `ECMAScript` ){
        alert("Right");
    }else{
        alert("Didn’t know? ECMAScript!");
    }
